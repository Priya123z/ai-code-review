name: ai-review scan

# Demonstrates the live pipeline: on every push/PR the AI reviews the demo
# module and uploads the HTML report as an artifact. On PRs it posts a summary
# comment. Skips gracefully when no provider secret is present, which is the case on forks.

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: write

jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install
        run: pip install -e .

      - name: Check secret
        id: secret
        env:
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
        run: |
          # Read through env rather than interpolating the secret into the test.
          if [ -n "$GROQ_API_KEY" ] || [ -n "$OPENROUTER_API_KEY" ]; then
            echo "present=true" >> "$GITHUB_OUTPUT"
          else
            echo "present=false" >> "$GITHUB_OUTPUT"
            echo "::warning::no GROQ_API_KEY or OPENROUTER_API_KEY, skipping the live scan."
          fi

      - name: Run AI scan on demo module
        if: steps.secret.outputs.present == 'true'
        env:
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
        run: |
          ai-review scan examples/flask_shop --out report --project "flask_shop (CI demo)" \
            --emit-tests report/generated_tests
          {
            echo "## AI code review"
            echo ""
            python - <<'PY'
          import json
          r = json.load(open("report/report.json"))
          f = [x for fr in r["files"] for x in fr["findings"]]
          sev = {}
          for x in f: sev[x["severity"]] = sev.get(x["severity"], 0) + 1
          print(f"**{len(f)} findings**: " + ", ".join(f"{k}: {v}" for k, v in sev.items()))
          print()
          for x in f[:10]:
              print(f"- `{x['severity']}` **{x['title']}**: {x['detail']}")
          PY
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Upload report artifact
        if: steps.secret.outputs.present == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: code-review-report
          path: report/
          retention-days: 14

      - name: Comment on PR
        if: github.event_name == 'pull_request' && steps.secret.outputs.present == 'true'
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const r = JSON.parse(fs.readFileSync('report/report.json', 'utf8'));
            const f = r.files.flatMap(fr => fr.findings);
            const sev = {};
            for (const x of f) sev[x.severity] = (sev[x.severity]||0)+1;
            const line = Object.entries(sev).map(([k,v]) => `${k}: ${v}`).join(', ');
            let body = `## AI code review\n\n**${f.length} findings**: ${line}\n\n`;
            for (const x of f.slice(0,10)) body += `- \`${x.severity}\` **${x.title}**: ${x.detail}\n`;
            body += `\n_Model: ${r.model}. Full HTML report in the run artifacts._`;
            await github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body
            });
